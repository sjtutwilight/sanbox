apiVersion: v1
kind: Service
metadata:
  name: load-executor
  namespace: platform-apps
  labels:
    app.kubernetes.io/name: load-executor
    app.kubernetes.io/component: executor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: load-executor
  ports:
    - name: http
      port: 18082
      targetPort: http
